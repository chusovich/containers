services:
  beszel-agent:
    image: henrygd/beszel-agent:0.18.4
    container_name: beszel-agent
    restart: unless-stopped
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./beszel_agent_data:/var/lib/beszel-agent
      # monitor other disks / partitions by mounting a folder in /extra-filesystems
      # - /mnt/disk/.beszel:/extra-filesystems/sda1:ro
    environment:
      LISTEN: 45876
      KEY: 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPW2VmL6jQHq/AaudZa5t1XeB1rdaVf8bsUG15IOSwa8'
      TOKEN: 3e93eb12-3eb0-494b-b6e3-3fed73fda079
      HUB_URL: https://beszel.services.husovich.com
